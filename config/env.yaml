# 多环境配置文件
# 可切换：test（测试）、dev（开发）、prod（生产）
test:
  # 公共基础地址
  base_url: "https://the-internet.herokuapp.com"
  # UI自动化测试地址
  ui_url: "https://www.selenium.dev/selenium/web"
  # 接口自动化测试地址
  api_url: "https://httpbin.org/post"